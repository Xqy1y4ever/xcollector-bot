# Xcollector bot —— 可选的 LLM 抽取依赖
#
#     .\.venv\Scripts\python.exe -m pip install -r requirements-llm.txt
#
# 只有在 `EXTRACTOR=llm` 或 `EXTRACTOR=both` 时才需要它。
# `EXTRACTOR=rule` 时 bot 完全不 import litellm（见 pipeline/extract.py 里的延迟导入），
# 所以没装它也不会影响服务启动和整条核心链路。

-r requirements.txt

# 多厂商统一调用（deepseek / openai / ...）。记得配好对应的 API key 环境变量
litellm>=1.40.0
