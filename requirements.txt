# Xcollector bot —— 核心依赖（Python 3.12+）
# 这个服务只做两件事：独占 OneBot 连接 + 处理私聊指令。
# 刻意不引入数据库 / LLM / 其它 Web 框架：bot 不该有状态，有状态的部分都在 backend。

fastapi>=0.115.0
uvicorn[standard]>=0.30.0
websockets>=12.0,<15.0
httpx>=0.27.0
pydantic>=2.7.0
pydantic-settings>=2.3.0
python-dotenv>=1.0.0
# Windows 没有系统 tzdata，ZoneInfo("Asia/Shanghai") 会失败
tzdata>=2024.1
