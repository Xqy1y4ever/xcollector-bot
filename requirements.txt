# Xcollector bot —— 核心依赖（Python 3.12+）
#
# 这个仓库现在是**消息处理层**：连 OneBot、筛选、抽取、digest、附件、统计全在这里，
# 后端退化成纯 CRUD 数据层。
#
# ⚠️ LLM 抽取所需的依赖**已经全在这里了**：多厂商调用走 app/llm/ 里自己写的
#    网关（支持 OpenAI 格式和 Google 原生格式），它只用到 httpx。
#    所以没有 requirements-llm.txt 这个东西，也没有构建开关 —— 一次装完即可。

fastapi>=0.115.0
uvicorn[standard]>=0.30.0
websockets>=12.0,<15.0
httpx>=0.27.0
pydantic>=2.7.0
pydantic-settings>=2.3.0
python-dotenv>=1.0.0
# Windows 没有系统 tzdata，ZoneInfo("Asia/Shanghai") 会失败；抽取的时间解析全靠它
tzdata>=2024.1
