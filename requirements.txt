# Xcollector bot —— 核心依赖（Python 3.12+）
#
# 这个仓库现在是**消息处理层**：连 OneBot、筛选、抽取、digest、附件、统计全在这里，
# 后端退化成纯 CRUD 数据层。
#
# ⚠️ 这里**刻意不含 litellm**：核心链路在 EXTRACTOR=rule 时不碰任何模型，
#    所以不装 litellm 也能把「收到消息 → 入库 → 建通知 → digest」整条跑通。
#    要跑 LLM 抽取请装：
#        .\.venv\Scripts\python.exe -m pip install -r requirements-llm.txt

fastapi>=0.115.0
uvicorn[standard]>=0.30.0
websockets>=12.0,<15.0
httpx>=0.27.0
pydantic>=2.7.0
pydantic-settings>=2.3.0
python-dotenv>=1.0.0
# Windows 没有系统 tzdata，ZoneInfo("Asia/Shanghai") 会失败；抽取的时间解析全靠它
tzdata>=2024.1
